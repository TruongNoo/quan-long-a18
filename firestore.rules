rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép người dùng đã đăng nhập đọc và ghi toàn bộ dữ liệu
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
